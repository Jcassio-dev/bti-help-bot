{
  "compilerOptions": {
    // ...existing compilerOptions...
    "outDir": "./dist", // Adicione ou modifique esta linha
    "rootDir": "./src", // Adicione esta linha para especificar a raiz dos seus fontes
    // ...existing compilerOptions...
    "skipLibCheck": true
  },
  "include": [
    "src/**/*" // Garanta que apenas a pasta src seja incluída na compilação
  ],
  "exclude": [
    "node_modules",
    "dist"
    // Adicione outros padrões de exclusão se necessário
  ]
}